global:
  scrape_interval: 15s  # How often to scrape targets by default.

scrape_configs:
  - job_name: "fastapi"
    static_configs:
      - targets: ["localhost:8000"]  # Your FastAPI app's metrics endpoint.
        labels:
          service: "fastapi-metrics"